library oxygene;

import 'package:flutter/material.dart';

class Oxygene extends StatelessWidget {
  const Oxygene({ super.key });

  @override
  Widget build(BuildContext context) {
    return Container(color: const Color(0xFF2DBD3A));
  }
}
